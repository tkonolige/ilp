-- this is a comment

-- this is a fact
full(john).
full(joe).

-- this is a rule
happy(X) :- full(X).

-- { R } => G is the syntax for implication
would_be_happy(X) :- { full(X) } => happy(X).

-- locals
foo(X) X Y :- Y = Z.

-- :+ will add a rule to the database in the repl
-- :+ full(me).
